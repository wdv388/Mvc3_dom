@model IEnumerable<Mvc3_dom.Models.Hotwater>

<table>
    <tr>
        
        <th>
            Previous
        </th>
       
        <th>
            date
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.CurrentTotal)
        </td>
       
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
       
    </tr>
}

</table>
