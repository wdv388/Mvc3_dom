@model IEnumerable<MvcHOME.Cold_Water>
<script src="~/Scripts/bootstrap.min.js"></script>
<link href="~/Content/bootstrap-theme.min.css" rel="stylesheet" />
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<script src="~/Scripts/jquery-1.10.2.js"></script>
<script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
<script src="~/Scripts/jquery-2.1.1.min.js"></script>
@{
    ViewBag.Title = "Index";
}

<h2>Холодна вода</h2>
<div>
    @using (Ajax.BeginForm("ShowFilter", new AjaxOptions { UpdateTargetId = "result" }))
    {
        Html.DropDownList("ID", ViewBag.Hom_ID as SelectList);
      //  Html.DropDownList("HomID", String.Empty);
    }
    
    
</div>
 @Html.DropDownList("ID", ViewBag.Hom_ID as SelectList)
<div id="result"></div>
<p>
   @* @Html.ActionLink("Додати нові", "Create")*@
</p>

<div class="row"> 
    <div class="col-md-6"> 
<table class="table table-striped table-bordered">
    <tr>
        <th>
          Лічильник 1
        </th>
        <th>
          Лічильник2 
        </th>
        <th>
          Разом за лічильником 1 та 2 
        </th>
        <th>
         Попередні показники
        </th>
        <th>
          Різниця
        </th>
        <th>
          Разом до сплати
        </th>
        <th>
          Квартира № 
        </th>
        <th>
         Дата 
        </th>
        @*<th></th>*@
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CW1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CW0)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CWT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CWP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CWD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hom.Apartament_naber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @Html.ActionLink("Редагувати", "Edit", new { id=item.ID }) |
           @* @Html.ActionLink("Details", "Details", new { id=item.ID }) |*@
            @Html.ActionLink("Видалити", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
        </div>
    </div>