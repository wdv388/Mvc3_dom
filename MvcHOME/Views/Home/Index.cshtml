@model IEnumerable<MvcHOME.Hom>

@{
    ViewBag.Title = "Index";
}

<h2>Квартири</h2>

<p>
    @Html.ActionLink("Додати нову квартиру", "Create")
</p>
<div class="row"> 
    <div class="col-md-6"> 

<table class="table table-striped table-bordered">
    <tr>
        <th>
           Квартира
        </th>
        <th>
          Пільга
        </th>
        <th>
           Газ
        </th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Apartament_naber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Benefit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gas)
        </td>
        <td>
                   <div class="btn-group">
    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
         Внести зміни <span class="caret"></span>
    </button>
 <ul class="dropdown-menu" role="menu">
     <li class="alert alert-info">@Html.ActionLink("Edit", "Edit", new { id=item.ID }) </li>
     <li class="alert alert-info">@Html.ActionLink("Details", "Details", new { id=item.ID }) </li>
     <li class=" alert-danger">@Html.ActionLink("Delete", "Delete", new { id=item.ID })  </li>
            
       </ul>
</div>       
       
        </td>
        <td>
            <div class="btn-group">
    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
        Додати показники <span class="caret"></span>
    </button>
                 <ul class="dropdown-menu" role="menu">
        <li>  @Html.ActionLink("+ холодна вода", "Create", "Cold", new {id=item.ID },null)  </li> 
        <li> @Html.ActionLink("+ гаряча вода", "Create", "Hot", new {id=item.ID },null)   </li>
        <li> @Html.ActionLink("+ електрика", "Create", "Electricity", new {id=item.ID },null) </li>
        <li>  @Html.ActionLink("+ стоки гарячої води", "Create", "Sewage", new {id=item.ID },null)</li>
                     <li>@Html.ActionLink("+ газ/тепло","Create","Gaz",new { id = item.ID },null) </li>

       @* <li class="divider"> </li>
        <li>  </li>
        <li>   </li>*@

        
    </ul>
</div>
        </td>
        <td>
            <div class="btn-group">  
                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
                    Поточні  показники споживання <span class="caret"></span>
                </button>
            <ul class="dropdown-menu" role="menu">
                <li>@Html.ActionLink("до показників споживання","Index","Records",new{id=item.ID},null) </li>
                <li></li>
            </ul>
          </div>
        </td>
        <td>
            <div class="btn-group">
                   <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
                      Тарифи та ліміти <span class="caret"></span>
                   </button>
                     <ul class="dropdown-menu" role="menu">
                    <li>@Html.ActionLink("тариф по квартирі", "Details", "Tarrif", new {id=item.ID },null) </li>
                    <li>@Html.ActionLink("ліміти", "Index", "Limit", new  {id=item.ID },null)</li>
                    <li>@Html.ActionLink("пільги", "Index", "Benefit", new  {id=item.ID },null)</li>
                </ul>
            </div>
        </td>

    </tr>
}

</table>
     </div>
  </div>
