@model IEnumerable<MvcHOME.Benefit>

@{
    ViewBag.Title = "Index";
}

<h2>Пільги</h2>

<p>
    @Html.ActionLink("Додати+", "Create")
</p>
<table class="table table-striped table-bordered">
    <tr>
        <th>
            @Html.Label("Пільга у %")
        </th>
        <th>
            @Html.Label("Осіб на пільгу")
        </th>
        <th>
            @Html.Label("Пільгових кВтг")
        </th>
        <th>
            @Html.Label("Холодна вода на 1 особу")
        </th>
        <th>
            @Html.Label("Гаряча вода на 1 особу")
        </th>
        <th>
            @Html.Label("Каналізація на 1 особу")
        </th>
        <th>
            @Html.Label("Квартира")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Privilege)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Persons)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.E)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CW)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HW)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.S)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hom.Apartament_naber)
        </td>
        <td>
             <div class="btn-group">
    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
         Внести зміни <span class="caret"></span>
    </button>
 <ul class="dropdown-menu" role="menu">
     <li>@Html.ActionLink("Edit", "Edit", new { id=item.ID }) </li>
     <li>  @Html.ActionLink("Details", "Details", new { id=item.ID })</li>
     <li> @Html.ActionLink("Delete", "Delete", new { id=item.ID }) </li>
            </ul>
           </div>
        </td>
    </tr>
}

</table>
